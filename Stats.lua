function Stats(cost, attack, defense, ability, border, rarity, ability_code, lore)
    return {
        cost = cost,
        attack = attack,
        defense = defense,
        ability = ability,
        border = border,
        rarity = rarity,
        ability_code = ability_code,
        lore = lore
    }
end

return Stats